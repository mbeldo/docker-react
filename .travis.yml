language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t mbeldo/docker-react -f Dockerfile.dev . 

script:
    - docker run -e CI=true mbeldo/docker-react npm run test -- --coverage